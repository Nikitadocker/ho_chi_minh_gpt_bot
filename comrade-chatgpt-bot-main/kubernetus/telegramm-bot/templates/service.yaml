apiVersion: v1
kind: Service
metadata:
  name: {{ include "telegramm-bot.fullname" . }}
  labels:
    {{- include "telegramm-bot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "telegramm-bot.selectorLabels" . | nindent 4 }}
